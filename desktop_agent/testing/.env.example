# Zmienne pod lokalne testy (skopiuj do testing/.env i dostosuj).
# Uwaga: aplikacja agenta trzyma konfigurację w electron-store (GUI),
# a te zmienne używa TYLKO skrypt seed.mjs oraz — opcjonalnie — debug w VS Code.

MSSQL_HOST=localhost
MSSQL_PORT=1433
MSSQL_USER=sa
MSSQL_PASSWORD=Wapro_Test_123!
MSSQL_DATABASE=WAPRO

# Włącza dodatkowe logi diagnostyczne procesu synchronizacji (jeśli podłączone).
WAPRO_DEBUG=1

# --- Klucze kanałów (do testu wysyłki; do samego czytania stanów niepotrzebne) ---
# BASELINKER_TOKEN=
# BASELINKER_INVENTORY_ID=
# BASELINKER_WAREHOUSE_ID=0
